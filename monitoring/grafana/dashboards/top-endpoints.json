{
  "dashboard": {
    "id": null,
    "uid": "modheshwari-top-endpoints",
    "title": "Top Endpoints",
    "panels": [
      {
        "type": "graph",
        "title": "Top 10 Endpoints by RPS",
        "targets": [
          { "expr": "topk(10, sum by (route) (rate(http_requests_total[5m])))", "refId": "A" }
        ]
      },
      {
        "type": "table",
        "title": "Top Endpoints p95",
        "targets": [
          { "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket[5m])) by (le, route))", "refId": "A" }
        ]
      }
    ]
  },
  "overwrite": false
}
