{
  "name": "ws",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "ws": "^8.19.0"
  },
  "scripts": {
    "dev": "bun run index.ts",
    "build": "tsc --outDir dist --module esnext --target esnext",
    "worker:notifications": "bun run worker.ts"
  }
}
